spring:
  config:
    import: aws-secretsmanager:rds-db-credentials/animoresdbinstatnce1/admin/1702189030443
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  datasource:
    url: ${url}
    username: ${username}
    password: ${password}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect